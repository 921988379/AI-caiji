# WP 采集助手操作说明

本文档适用于 WordPress 插件 **WP 采集助手（wp-caiji）**。

## 基本流程

1. 进入 **WP 采集 → 设置** 配置全局参数。
2. 进入 **WP 采集 → 采集规则** 新增或编辑规则。
3. 填写列表页 URL、文章链接规则、标题/正文/日期规则。
4. 保存规则后使用“测试发现链接”和“测试预览”。
5. 确认无误后点击“发现链接”“采集队列”，或等待定时任务自动执行。

## 2.1.20 更新说明

- **修复自动发布标签写入**：修复自动采集/Cron 发布文章时 Tag 标签可能没有真正写入文章的问题，改为文章创建成功后显式写入 `post_tag`。
- **增强固定标签分隔**：固定标签现在支持英文逗号、中文逗号、顿号、分号、竖线、斜杠和换行等多种分隔符。
- **修复规则导入导出**：补上 `auto_tag_advanced` 增强匹配开关，避免导出再导入后自动标签匹配方式丢失。

## 2.1.19 更新说明

- **修复 HTTP 自定义端口**：修复 AI Endpoint 使用 HTTP 自定义端口时被 WordPress 默认安全端口校验拦截的问题。
- **支持公网自定义端口**：现在可使用公网 HTTP/HTTPS 自定义端口的 OpenAI 兼容 Endpoint。
- **保留安全校验**：仍会拒绝 localhost、内网 IP 和保留地址，避免误请求本机或内网服务。

## 2.1.18 更新说明

- **AI API 支持 HTTP**：AI Endpoint 现在兼容公网 HTTP 地址，便于连接部分不支持 HTTPS 的 OpenAI 兼容中转服务。
- **保留自动补全**：仍可填写基础地址、`/v1` 或完整 `/chat/completions` 地址，插件会自动补全为 OpenAI 兼容接口。
- **安全建议**：生产环境仍建议优先使用 HTTPS；HTTP 仅建议用于可信网络或临时调试场景。

## 2.1.17 更新说明

- **Referer 自动生成**：采集规则里的 Referer 留空时，会自动随机生成来源页，包含目标站首页/栏目页和常见搜索引擎来源。
- **Cookie 池随机选择**：Cookie 输入框支持每行填写一组 Cookie，采集时随机选择一组发送。
- **安全默认策略**：Cookie 留空时不发送 Cookie，避免生成无效随机 Cookie 触发目标站风控。

## 2.1.16 更新说明

- **自动生成 User-Agent**：当采集规则里的 User-Agent 池留空时，系统会自动随机生成常见浏览器 User-Agent，不再使用固定插件标识。
- **覆盖常见设备与浏览器**：内置 Chrome、Edge、Safari、Android Chrome、iPhone Safari 等访问标识，降低默认请求头过于固定的问题。
- **兼容手动配置**：如果你在规则里填写了 User-Agent 池，仍然优先从手动列表随机选择。

## 2.1.15 更新说明

- **修复自动标签兼容提示**：修复发布文章时旧队列数据缺少 `auto_tag_advanced` 字段导致 PHP 未定义索引提示的问题。
- **增强升级兼容性**：自动标签增强匹配开关在缺省数据下会安全回退为关闭状态，避免升级后采集发布流程被警告信息干扰。

## 2.1.14 更新说明

- **修复自动标签无法保存**：修复部分站点在“分类与标签 → 自动标签”中保存后重新打开看不到设置的问题，补齐 `auto_tag_advanced` 数据库列升级逻辑。
- **增强规则保存兼容性**：保存采集规则时会先按当前数据表字段过滤，避免未来新增字段未同步升级时导致整条规则保存失败。
- **新增后台版权署名**：在插件后台页面统一显示版权署名“[一点优化](https://www.seoyh.net/)”。

## 2.1.13 更新说明

- **自动标签增强匹配开关**：在 **采集规则 → 分类与标签 → 自动标签** 中新增“启用增强匹配”开关。开启后使用增强匹配；关闭后回退为旧版纯包含匹配。
- **自动标签支持映射写法**：自动标签关键词支持 `关键词=>标签名` 和 `关键词1|关键词2=>标签名`，便于把多个别名统一归一到同一个 Tag。
- **英文短词误匹配优化**：增强匹配模式下，像 `AI`、`SEO`、`WP` 这类英文短关键词会尽量按整词匹配，减少误伤其他单词片段。
- **标签规范化去重**：自动标签最终写入前会做更稳妥的规范化去重，减少大小写差异或重复写法导致的脏标签。

## 2.1.12 更新说明

- **编辑采集规则**：来源与文章 URL、文章字段提取不再一次性展开全部三种获取规则，改为先选择“选择器 CSS/XPath / 前后代码截取 / JSON 路径”，再显示对应编辑项。
- **随机延迟采集**：在 **WP 采集 → 设置 → 定时与队列 → 采集文章频率** 中，可以选择“随机延迟采集”，并设置最小/最大分钟数。启用后，每次采集完成会重新随机安排下一次采集文章任务。
- **设置页收起**：**WP 采集设置** 页面各板块默认收起，点击板块标题后展开，减少后台页面占用。

## 前后代码截取功能

规则编辑里的“基础采集”支持多处“前后代码截取”：

- **文章链接**：用于列表页/栏目页，从指定片段中提取文章 URL。
- **标题**：用于文章详情页，从指定片段中提取标题。
- **正文**：用于文章详情页，从指定片段中提取正文 HTML。
- **日期/时间**：用于文章详情页，从指定片段中提取发布时间。

### 使用逻辑

1. 如果某字段填写了“前代码/开始标记”或“后代码/结束标记”，插件会先从原始 HTML 中截取对应片段。
2. 如果该字段的选择器有填写，会在截取后的片段里继续用 CSS/XPath 选择器提取。
3. 如果选择器留空：
   - 文章链接：自动提取片段内所有 `<a href="...">` 链接。
   - 标题/日期：直接使用片段去除 HTML 后的文本。
   - 正文：直接使用片段 HTML。
4. 可以只填前代码，也可以只填后代码；两个都填时提取中间区域。
5. 前后代码匹配不到时，该字段会提取为空，建议用“测试预览”检查。

### 示例：正文前后截取

前代码：

```html
<div class="article-content">
```

后代码：

```html
<div class="article-footer">
```

正文选择器：

```css
.article-content
```

如果截取后片段本身就是正文 HTML，也可以把“正文选择器”留空。

### 示例：标题前后截取

前代码：

```html
<h1 class="title">
```

后代码：

```html
</h1>
```

标题选择器可以留空，插件会直接取中间文本。


## Tag 标签配置

从 2.1.4 起，规则支持两类标签能力：

### 文章字段提取里的 Tag 标签

在 **编辑采集规则 → 文章字段提取** 中可以配置：

- **Tag 标签选择器**：例如 `.tags a` 或 `//a[@rel='tag']`。
- **Tag 标签前后代码**：先截取标签区域，再提取标签文本。
- **Tag 标签 JSON 路径**：适合标签存放在页面 JSON 中的站点。

提取到的标签会在发布文章时自动合并到 WordPress 文章标签中，并与固定标签、自动标签去重。多个标签支持用逗号、顿号、分号、竖线、斜杠或换行分隔。

### 分类与标签里的自动标签

在 **编辑采集规则 → 分类与标签 → 自动标签** 中，文本框会自动载入当前站点已有的文章 Tag。你可以继续手动增删，每行一个关键词/标签。

自动标签匹配规则：

- 只有 **文章标题** 中出现该词时，才会自动作为标签。
- 正文中出现该词不会触发自动标签。

这样可以避免正文里偶然出现某个词导致标签过多或不准确。

## 注意事项

- 采集 URL 仍会经过安全校验，只允许公网 http/https 地址。
- 测试按钮不会保存当前修改；刚改规则后请先保存再测试。
- 如果选择器和前后代码同时填写，实际匹配范围是“先截取、再选择器”。


## 健康检查：插件更新检测

从 2.1.5 起，**WP 采集 → 健康检查** 会显示当前插件版本和 GitHub Release 最新版本。

如果刚发布新版本但后台还没有提示更新，可以点击维护工具里的 **手动检查插件更新**。该按钮会：

- 清理插件自己的 GitHub Release 缓存。
- 清理 WordPress 插件更新缓存。
- 立即请求 GitHub Releases 列表，并按版本号识别最高正式版本。
- 在健康检查页面显示是否已是最新版本，或是否发现新版本。

如果目标服务器无法访问 GitHub API，健康检查会显示检查失败提示。

从 2.1.6 起，更新检测不再单纯依赖 GitHub `/releases/latest`，而是读取 Releases 列表并按版本号选择最高正式版本，避免旧 Release 被误判为最新版。

## JSON / Next.js 采集

从 2.1.3 起，规则支持从页面中的 JSON 数据提取列表链接和文章字段，适合 Next.js、JSON-LD 或把内容预置在 `<script>` 里的站点。

### 支持的 JSON 来源

在“JSON/Next.js 链接”的“JSON 来源”里可填写：

- `__NEXT_DATA__`：默认，读取 `<script id="__NEXT_DATA__">`。
- `ld+json`：读取 `type="application/ld+json"` 的结构化数据。
- `id:脚本ID`：按指定 script id 读取 JSON，例如 `id:__NUXT_DATA__`。
- `var:变量名`：读取页面脚本变量，例如 `var:__INITIAL_STATE__` 或 `var:window.__INITIAL_STATE__`。

如果 JSON 来源、JSON 路径没有填写，插件会继续使用原来的选择器和前后代码截取，不影响旧规则。

### JSON 路径写法

支持常用点路径：

```text
props.pageProps.data.title
```

也支持数组下标：

```text
items[0].title
```

列表链接通常填写数组路径，再填写 URL 字段：

```text
JSON 路径：props.pageProps.corporateData
URL 字段：alias
```

### The Edge Malaysia 示例

列表页 URL：

```text
https://theedgemalaysia.com/categories/corporate
```

JSON/Next.js 链接：

```text
JSON 来源：__NEXT_DATA__
JSON 路径：props.pageProps.corporateData
URL 字段：alias
```

文章字段：

```text
标题 JSON 路径：props.pageProps.data.title
正文 JSON 路径：props.pageProps.data.content
日期 JSON 路径：props.pageProps.data.created
```

该站点的文章 alias 类似 `node/803289`，插件会自动转换为完整公网 URL，例如：

```text
https://theedgemalaysia.com/node/803289
```

日期如果是 13 位毫秒时间戳，发布时会自动转换成 WordPress 可识别的时间。


### 默认改写 Prompt 更新

从 2.1.7 起，默认 AI 改写 Prompt 增加以下要求：

- 正文中需要数据的段落，要求 AI 在网上找到对应数据，并在数据下面说明数据来源和 RUL。
- 正文尾部增加 FAQ。
- 仍要求只返回 JSON：`{"title":"改写后的标题","content":"改写后的 HTML 正文"}`。

升级时，如果站点当前 Prompt 为空或仍是旧版默认 Prompt，插件会自动替换为新版默认 Prompt；如果管理员已经自定义过 Prompt，则不会覆盖。


### AI API Key 明文保存

从 2.1.8 起，AI API Key 改为明文保存，并在 **WP 采集 → 设置 → AI 改写设置** 中直接显示。

- 只有拥有插件设置权限的后台管理员可以查看和修改。
- 旧版本已加密保存的 Key 会在升级后自动转换为明文保存。
- 诊断导出仍会自动脱敏 AI API Key，避免误泄露。


### 自动标签匹配范围

从 2.1.9 起，自动添加文章 tag 标签只匹配文章标题：

- 标题中出现自动标签关键词时，才会自动作为文章 tag。
- 正文内容中出现关键词，不会自动作为文章 tag。
- 分类匹配不受影响，仍可按标题 + 正文内容匹配分类规则。


### AI 改写目标语言与语言检测

从 2.1.10 起，AI 改写支持设置目标语言，适合翻译采集内容后再发布：

- 在 **WP 采集 → 设置 → AI 改写设置** 中可设置全局默认改写语言。
- 在单条采集规则的 **AI 改写** 区域，可单独覆盖目标语言。
- 支持中文、英文、日文、韩文、西班牙文、法文、德文，以及“不限制/不检测”。
- 启用“检测改写后语言”后，如果 AI 返回的标题和正文没有通过目标语言检测，会判定为 AI 改写失败。
- 判定失败后，会按该规则的“失败处理”执行：可以发布原文，也可以把队列标记为失败。


### 自动标签关键词

从 2.1.11 起，“编辑采集规则 → 分类与标签 → 自动标签”的关键词输入框不会再自动载入站点已有标签，只使用管理员手动填写并保存的关键词。自动标签仍然只按文章标题匹配，正文命中不会自动加标签。
